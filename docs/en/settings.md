# Settings

TODO: List each setting in the configuration dialog, with the exact
Names from the dialog, what it does, what value is preset, and — 
if relevant — which values are allowed. 

| Setting | Description |
| --- | --- |
| _(name)_ | _(description)_ |

TODO: If settings depend on each other (e.g. field only works in one
certain mode), say that explicitly here.